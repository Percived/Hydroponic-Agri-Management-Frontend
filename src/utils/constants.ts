/**
 * 分页相关常量
 */

// 默认页面大小
export const DEFAULT_PAGE_SIZE = 20

// 常用页面大小选项
export const PAGE_SIZE_OPTIONS = [10, 20, 50, 100]

// 大量数据页面大小（用于下拉选择等场景）
export const LARGE_PAGE_SIZE = 100

// 超大数据页面大小（用于全量加载场景）
export const EXTRA_LARGE_PAGE_SIZE = 1000

/**
 * 刷新间隔相关常量（单位：秒）
 */
export const REFRESH_INTERVAL_OPTIONS = [10, 30, 60]
export const DEFAULT_REFRESH_INTERVAL = 30
